import { CategoryRecipeGroup } from './category-recipe-group';

describe('CategoryRecipeGroup', () => {
  it('should create an instance', () => {
    expect(new CategoryRecipeGroup()).toBeTruthy();
  });
});
